{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "dev server",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/main.go",
            "buildFlags": ["-tags=dev"]
        }
    ]
}